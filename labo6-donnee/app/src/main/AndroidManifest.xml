<?xml version="1.0" encoding="utf-8"?>
<!--
    Nom du fichier : AndroidManifest.xml
    Description    : Déclare les composants principaux de l'application, les permissions requises
                     et les métadonnées importantes. Inclut l'activité principale et
                     la configuration de l'application.
    Auteurs        : Bugna, Slimani & Steiner
    Date           : 08 janvier 2025
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission INTERNET ajoutée ici -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Labo6"
        tools:targetApi="33"
        android:name=".ContactsApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>